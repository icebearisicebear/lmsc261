const activities = ['babysit tadpoles', 'flies for lunch',  'tongue stretch', 'swimming lesson'];
let activityIndex = prompt('Dial in the number of your desired activity:') % 4;
print(activities[activityIndex]);
